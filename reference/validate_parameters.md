# Validate algorithm parameters

Validate algorithm parameters

## Usage

``` r
validate_parameters(alpha, rho, threshold)
```

## Arguments

- alpha:

  IBP concentration parameter

- rho:

  Dictionary sparsity parameter

- threshold:

  Single infection threshold

## Value

TRUE if valid, otherwise throws error
