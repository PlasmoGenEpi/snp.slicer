# Calculate allele counts for a single sample

Calculate allele counts for a single sample

## Usage

``` r
calculate_allele_counts_single(
  A,
  D,
  snp_indices,
  r0_values,
  r1_values,
  sep = "|"
)
```
