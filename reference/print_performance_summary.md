# Print performance summary

Print performance summary

## Usage

``` r
print_performance_summary(summary = get_performance_summary())
```

## Arguments

- summary:

  Performance summary data frame. Defaults to the log of the current
  process; pass a chain's stored `performance` table when the chain ran
  in a separate process.
