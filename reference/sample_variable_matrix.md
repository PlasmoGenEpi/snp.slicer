# Build the invariant-summary matrix for one chain

Build the invariant-summary matrix for one chain

## Usage

``` r
sample_variable_matrix(samples, pars)
```

## Arguments

- samples:

  Retained samples for one chain

- pars:

  Parameters to extract

## Value

Numeric matrix, one row per sample and one named column per (expanded)
variable
