# Initialize slice sampler

Initialize slice sampler

## Usage

``` r
slice_init(state, model_obj)
```

## Arguments

- state:

  Initial state

- model_obj:

  Model object

## Value

State with slice sampler initialized
